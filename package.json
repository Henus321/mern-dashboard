{
  "name": "mern-dashboard",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm run server:dev\" \"cd frontend && npm run start\" ",
    "prod": "concurrently \"cd backend && npm run server:prod\" \"cd frontend && npm run build\" "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Henus321/mern-dashboard.git"
  },
  "keywords": [],
  "author": "Alexander Erkhov",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}
